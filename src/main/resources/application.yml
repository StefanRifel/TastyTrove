server:
  port: 8082

spring:
  datasource:
    url: database
    username: username
    password: password
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database: mysql
    database-platform: org.hibernate.dialect.MySQLDialect

app:
  jwtSecret: ======================TastyTrove=Spring===========================
  jwtExpirationMs: 86400000
